# Model weights - downloaded via scripts/download_weights.sh, far too large for git
weights/
*.pth
*.pt

# Pipeline inputs and outputs
input/
output/
out/
*_timeseries_canonical.csv
*_interpretation.json

# ...except the integration check's stored reference: a fixture, not a run's output. See
# tests/integration/README.md.
!tests/integration/expected/*_timeseries_canonical.csv

# Patient data must never be committed
data/
*.dcm

# ECGs de muestra copiados desde el checkout del digitalizador, para demos.
# NO se versionan: son material CC BY-SA, y este repositorio se mantiene libre de
# el a proposito (ver NOTICE). Redistribuirlos sin modificar solo exigiria
# atribucion, pero la linea es mas facil de defender si no se cruza.
ejemplo-ecg-*.png

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.mypy_cache/
.pytest_cache/

# OS / editor
.DS_Store
.idea/
.vscode/
